# 任务：修复「重启 Helix 后对话无法使用」

## 症状
用户重启 Helix（Tauri 桌面应用）之后，之前的对话（会话）无法继续使用——恢复会话失败或历史不可用。你的工作：定位根因并修复。

## 项目背景
- 仓库：D:\project\helix（Tauri + Vite + React）
- 后端 agent 是外部 pi coding agent（npm 包），由 `src-tauri/src/pi_gateway.rs` 通过 `pi --mode rpc`（stdio JSONL RPC）spawn
- 会话持久化在 pi 侧（`~/.pi/agent/`，state.db / session 文件）；Helix 前端通过 `src/lib/serve-gateway.ts` 的 WS 网关与后端通信

## 相关代码线索（从哪开始查）
- `src-tauri/src/pi_gateway.rs`：`session/resume`（约 1232 行）、`session/prepare`（约 1191 行）、`instance_for_session`（约 1728 行）、`RESUME_LOCKS`
- `src/lib/serve-gateway.ts`：`session/resume` 分支（约 1140 行）；`stored_session_id` vs `ui_session` 的区别（约 1111 行，ui_session 进程重启即失效，stored_session_id 是 state.db 主键、跨重启存活）
- `src/lib/session-map.ts`、`src/lib/session-resync.ts`
- `src/stores/helix-store.ts`：启动时恢复上次会话（约 3917 行）
- `src/components/Helix/agent-flow-panel.tsx`：网关重启 / epoch 处理（约 2442–2540、3505、3751、4420 行）

## 要求
1. 先定位确切根因：梳理「重启 → 前端恢复上次会话 → 网关 resume → pi 进程恢复」整条链路，找出断在哪一步；必要时运行日志或加临时日志验证，但最终代码里不要留调试输出
2. 实现修复，保持最小改动，不要顺手重构无关代码
3. 验证：`cargo check`（在 src-tauri 下）通过；前端 `npx tsc --noEmit`（或项目现有的类型检查方式）通过
4. 把根因分析 + 改动摘要写入 `D:\project\helix\.tmp-pi-result.md`（中文）
5. 全部完成后，在最终回复的第一行输出 `TASK_DONE`，然后给出不超过 10 行的总结（改了哪些文件、根因是什么）

注意：仓库当前有未提交的改动（属于进行中的工作），不要丢弃、回退或覆盖它们；不要执行任何 git 破坏性操作（reset/checkout/clean 等）。

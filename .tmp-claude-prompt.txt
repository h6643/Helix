分析 Helix 项目（Electron+React 前端在 D:/Project/Helix，Rust/Tauri 后端在 D:/Project/Helix/src-tauri）中"切换项目（工作目录）时 UI 卡顿"的根因。只读分析，不要修改任何文件。

切换项目的执行链路（已定位，供你深入）：
1. 入口：src/components/Helix/sidebar.tsx 第 582/896 行调用 setWorkDir(dir)
2. 核心：src/stores/helix-store.ts 第 1956-2053 行 setWorkDir()：
   - await api.app.setWorkDir(relativePath)（Tauri/后端命令）
   - await electronFS.scanTree(absDir) 全量扫描目录树后 set({ files: tree })
   - 非运行状态分支会 setCurrentSessionId(null) + workDirEpoch+1
3. workDirEpoch bump 会触发 src/components/Helix/agent-flow-panel.tsx（4000+ 行大组件）里依赖它的 effect 重新执行/清 sessionMapRef

请重点分析：
A. setWorkDir 中 scanTree 是否串行阻塞、是否有超大目录/node_modules 未过滤导致大树 set() 引发全量重渲染
B. workDirEpoch + files 两个 state 更改是否可合并为一次 set()，减少渲染批次
C. agent-flow-panel.tsx 中依赖 workDirEpoch/selectedWorkDir/files 的 useEffect、useMemo、订阅链——切目录时哪些昂贵 effect 被无谓触发（git status 扫描、branch 查询等，见 2668-2700 行附近）
D. sidebar.tsx 里 useHelixStore 的大对象解构订阅（381 行附近）是否导致 sidebar 在 files 变化时整树重渲染
E. 后端 src-tauri 里 set_work_dir 命令的实现是否做了昂贵同步 IO（git 仓库探测、目录遍历等）

输出要求：按影响程度排序的卡顿根因清单，每条包含：代码位置(文件:行号)、机制解释、修复建议。用中文回答。

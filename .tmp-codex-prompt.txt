检查 Helix 项目（Electron+React 前端在 src/，Rust/Tauri 后端在 src-tauri/）的性能 bug，聚焦"切换项目/工作目录"相关链路。只读分析，不要修改任何文件。

重点检查以下文件中的性能问题：
1. src/stores/helix-store.ts（特别是 setWorkDir，约 1956-2053 行；以及所有订阅 files / selectedWorkDir / workDirEpoch 的 selector）
2. src/components/Helix/agent-flow-panel.tsx（约 4400 行，检查依赖 selectedWorkDir/workDirEpoch 的 useEffect/useMemo/订阅是否切目录时触发昂贵副作用，如 git branch 查询 2668-2700 行、文件树快照 4354 行）
3. src/components/Helix/sidebar.tsx（381 行起的大对象 useHelixStore 解构订阅）
4. src/components/Helix/file-tree-panel.tsx（files 大数组渲染是否缺 memo/virtualization）
5. src-tauri/ 中 set_work_dir 及相关 Tauri 命令的 Rust 实现（同步 IO、目录全量遍历、未过滤 node_modules 等）

要找的性能 bug 类型：
- 阻塞主线程的 await 串行链
- 缺失 memo/useMemo/useCallback 导致的级联重渲染
- zustand 大对象订阅导致的过度渲染
- set() 未合并导致的多次渲染批次
- React key 不稳定 / 列表全量重建
- Rust 侧同步阻塞 IO 或 O(n) 全量扫描
- 轮询/定时器未清理

输出要求：按严重程度排序的性能 bug 清单，每条包含 文件:行号、问题描述、影响、修复建议。用中文回答。

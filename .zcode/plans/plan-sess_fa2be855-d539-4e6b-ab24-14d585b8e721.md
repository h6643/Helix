## 目标与边界

按你选定的口径执行：**只做文件修改，不提交、不暂存（不碰 index）**。因此原表格里 `git rm --cached`、`git add` 三个组件、以及"git status 无已删未提交项"这条验收，都留给你提交时处理——我会在交付时明确说明哪些验收项需要 commit 才能达成。

已核实的事实（决定任务实际规模的关键点）：
- `eslint.config.mjs` 是 **tracked 且工作区已删**（112 行，eslint 9 扁平配置，含 `globalIgnores` 忽略自身、`pi/**`、`src-tauri/**`），`git checkout --` 可直接还原；eslint 及其 6 个插件都在 package.json devDeps 里，`lint: eslint .` 脚本仍在。
- `.classify.cjs` / `.unexport.cjs` **在磁盘上不存在**（全盘 maxdepth 2 搜索无结果），也未被 git 跟踪；`scripts/` 目录不存在，`.gitignore` 里已有一条注释说明 scripts/ 是 Helix 自己的目录。**这一项基本是空操作**，无需移动。
- `test/` 存在但为空且未被跟踪；`nul` 是 0 字节根目录文件，`.gitignore` 最后一行已有 `nul`（只需删物理文件）。
- `external.rs` 唯一命令是 `test_connection`，前端零调用；`mod external` 已从 lib.rs 删除；`src/` 下 `helix-facade` / `helixFacade` 引用为 0。删除是干净的。
- `lib.rs:312` 的 `// external (TCP probe, SSH)` 是残注释，其下方四条 `app::` 命令已归在上方的 `// app` 分组头下 → 直接删该行即可。
- README 模块表 28 行对照磁盘 28 个 `.rs` 文件：唯一失效行是 `external.rs`；`src-tauri/tests/trim_session.rs` 确实存在，那条测试描述保留。
- `src-tauri/resources/` **确实为空目录**（find 无文件）→ README 那行描述该删。
- `.github/workflows/build-windows.yml` 里**没有 lint 步骤**，README 的 CI 五步描述与之一致，无需改。

## 1.1 恢复 ESLint 配置 + 临时产物入 .gitignore

1. `git checkout -- eslint.config.mjs` 还原配置。
2. 在 `.gitignore` 追加一节（放在末尾「accidental diff/artifact files」区域之后）：
   ```
   # Lint / one-off inspection artifacts (never commit)
   eslint-report.json
   parse-eslint.mjs
   watch-log.ps1
   ```
3. **不做** `git rm --cached`（不碰 index）。三个文件工作区已删，加了 ignore 条目后不会再被跟踪。
4. 验收：`npm run lint` 能跑起来。注意标准是"可运行"而非零告警——当前有 60 个文件带未提交改动，eslint 可能报出一批真实告警，我会如实贴出结果，不擅自 `--fix`。

## 1.2 组件就位 + 清理零散产物

1. 三个未跟踪组件已存在于磁盘且被引用（`agent-flow-panel.tsx:99,117` 引 slash-commands / transcript-message，`right-sidebar.tsx:7` 引 byline-panel），新 clone 确实会构建失败——**无需改动，只提醒提交时 `git add` 这三个路径**。
2. `rm -rf test`（空目录、未跟踪）。
3. `rm -f nul`（0 字节、已被 ignore）。
4. `.classify.cjs` / `.unexport.cjs` 不存在、`scripts/` 不存在 → 跳过，在交付说明里注明核查结果。
5. 验收：`npx tsc --noEmit`（计划模式被拦，执行阶段跑）确认这三个模块的 import 全部解析；随后按表格跑 `npm run build`。

## 1.3 正式处理已删源码

1. `external.rs`、`helix-facade.ts` 保持删除（引用已确认为 0）。
2. `src-tauri/src/lib.rs` 第 312 行删除 `// external (TCP probe, SSH)` 一行；下方 `app::sync_work_dir` / `set_work_dir` / `get_data_root` / `set_data_root` 归入已有的 `// app` 分组。
3. 验收项 `git status 无"已删未提交"项` **在不提交的前提下无法满足**：` D src-tauri/src/external.rs` 与 ` D src/lib/helix-facade.ts` 会继续显示，直到你提交。可达成的是"引用干净、文件已删、仅剩 index 待提交"。

## 1.5 README 修订

1. 目录树第 73 行删掉 `│   ├── resources/               # 内嵌资源`（目录为空）。
2. Rust 模块表第 105 行删掉 `| external.rs | 网络 / SSH 连通性探测 |`。
3. 第 67 行 `lib/` 描述去掉 "facade"（`helix-facade.ts` 已删）：改为"Tauri/serve 桥接、持久化、MCP、工具展示等"。
4. 命令与 CI 段核对后**不动**：`npm run lint` 随 1.1 恢复配置而可用；CI 五步与实际 workflow（无 lint 步骤）一致；`trim_session.rs` 测试描述属实。
5. 验收：`grep -n "external.rs\|helix-facade\|resources/" README.md` 无结果。

## 提交

按你的选择，全程不执行 `git add` / `git commit`，改动全部留在工作区。交付时我会列出需要你在 commit 时 `git add` 的清单：`eslint.config.mjs`、`.gitignore`、`src-tauri/src/lib.rs`、`README.md`、三个组件路径，以及四处删除（`eslint-report.json`、`parse-eslint.mjs`、`watch-log.ps1`、`external.rs`、`helix-facade.ts`）。

需要执行的权限：还原被删文件（`git checkout --`）、删除 `test/` 与 `nul`、运行 `npm run lint` / `npx tsc --noEmit` / `npm run build` 做验收。
# Dependencies
node_modules/
.pnp
.pnp.js
.workbuddy/

# Testing
coverage/

# Next.js
.next/
out/

# Production
build/
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db

# Prisma
prisma/migrations/

# Standalone build
.next/standalone/

# Logs
*.log
server.log

# Database
*.db
*.sqlite

# Bun
bun.lockb
bun.lock

# Helix project config (may contain API keys)
helix.json
.helix/
.helix-snapshots/
.env*

AI tool
.mimocode/
.claude/


# Backup files
original.txt

# Tauri build output
src-tauri/target/

# Skills (user-specific, not shared)
skills/

# hermes-agent is now a git submodule (see .gitmodules). Its internal paths are
# no longer tracked by this repo — only the nested .git must stay untracked.

AGENTS.md

.playwright-mcp/

# accidental diff/artifact files
src/hermes.rs:*
~hermes/

# Hermes data dir mistakenly created at repo root (HERMES_HOME once pointed here).
# These are NOT Helix source — never commit them. NOTE: scripts/ is Helix's own dir (prepare-runtime.sh), do NOT ignore it.
config.yaml
auth.json
auth.json.bak
auth.lock
agent-extra/
audio_cache/
cron/
desktop/
hooks/
# 上条规则会误伤前端源码目录 src/hooks（前端 React hooks），这里显式放行。
!src/hooks/
image_cache/
lsp/
git/
bin/
logs/
cache/
channel_directory.json
channels.json
gateway_state.json
gateway-starts.log
.hermes_history

# Hermes runtime dotfiles left at repo root (HERMES_HOME once pointed here).
.mcp-discovery.lock
.no-bundled-skills
.npm_lock_hash_*
.processes_*.tmp
.skills_prompt_snapshot.json
.update_check

# nested hermes-agent .git must not be tracked (vendored dir keeps its own git)
hermes-agent/.git/

# KaTeX fonts copied to repo root by vite build
KaTeX_*

# codex CLI temp dir
.codex-tmp/
